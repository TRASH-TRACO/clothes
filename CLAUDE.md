@AGENTS.md

# 이 저장소에서 지킬 것

## supabase/schema.sql 을 건드렸으면 SQL 을 답변에 같이 붙인다

이 프로젝트는 마이그레이션 도구를 안 쓴다. 스키마를 바꾸면 사람이 Supabase 의
SQL Editor 에 직접 붙여넣어야 한다.

그러니 **`supabase/schema.sql` 을 고친 턴에는 붙여넣을 SQL 을 답변에 그대로 적는다.**
물어보고 주지 말고, 안 물어봐도 준다. "schema.sql 을 다시 실행하세요" 로 끝내지 않는다.

- 새로 더한 부분만 떼서 준다 (전체를 다시 돌려도 안전하지만, 매번 전체를 읽게 할 이유가 없다).
- 안 돌렸을 때 무엇이 안 되는지 한 줄로 같이 적는다. 대개 화면은 그대로 돌아가고
  새 기능만 빠지게 만들어 두므로, 그 사실을 알아야 급한지 아닌지 판단할 수 있다.
